// Deprecated in v3 — replaced by AppShell at src/components/AppShell.tsx.
// Kept as a no-op so any straggler imports still build.
export function DirectorNav(_props: { role: "DIRECTOR" | "ORG_ADMIN" }) {
  return null;
}
